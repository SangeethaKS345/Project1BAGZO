<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>


    <style>
       body {
           background-color: #f8f9fa;
       }

       .sidebar {
           background-color: #343a40;
           color: white;
           border-radius: 10px;
           height: 75vh;
       }

       .sidebar .nav-link {
           color: white;
           transition: 0.3s;
           font-size: 1.1rem;
       }

       .sidebar .nav-link:hover {
           background-color: #495057;
           border-radius: 5px;
       }

       .logout-link {
           color: #dc3545 !important;
           font-weight: bold;
       }

       .profile-card, .order-list {
           border-radius: 10px;
           background-color: white;
           box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
       }

       .order-list ul {
           list-style: none;
           padding: 0;
       }

       .order-list li {
           background: #f1f1f1;
           padding: 10px;
           margin: 5px 0;
           border-radius: 5px;
           color: #212529;
       }

       .welcome-box {
           background: #1e3a8a;
           color: white;
           padding: 20px;
           border-radius: 10px;
           text-align: center;
           font-weight: bold;
           font-size: 1.5rem;
           box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
       }

       /* Breadcrumbs Styling */
       .breadcrumb {
           background: transparent;
           font-size: 0.85rem;
           padding: 5px 0;
           margin-bottom: 10px;
       }

       .breadcrumb-item a {
           color: #6c757d; /* Subtle dark gray */
           text-decoration: none;
       }

       .breadcrumb-item.active {
           color: #343a40; /* Darker shade */
       }
    </style>
</head>
<body>
    
</div>
<div class="row mt-3">
    <div class="col-md-6">
        <strong>Phone:</strong> <%= userData.phone || 'Not provided' %>
    </div>
    <div class="col-md-6">
        <strong>Account Created:</strong> 
        <%= userData.createdAt ? new Date(userData.createdAt).toLocaleDateString() : 'N/A' %>
    </div>
</div>
</div>

<div class="profile-section">
<h3>Account Actions</h3>
<div class="row">
    <div class="col-md-4">
        <a href="/account" class="btn btn-primary w-100">Edit Profile</a>
    </div>
    <div class="col-md-4">
        <a href="/address" class="btn btn-secondary w-100">Manage Addresses</a>
    </div>
    <div class="col-md-4">
        <a href="/order-history" class="btn btn-info w-100">Order History</a>
    </div>
</div>
</div>

<div class="profile-section">
<h3>Security</h3>
<div class="row">
    <div class="col-12">
        <p>
            <strong>Login Method:</strong> 
            <%= userData.googleId ? 'Google Login' : 'Email/Password' %>
        </p>
    </div>
</div>
</div>
<% } else { %>
<div class="alert alert-warning text-center">
<p>Please log in to view your profile.</p>
<a href="/login" class="btn btn-primary">Log In</a>
</div>
<% } %>
</div>

<!-- Bootstrap JS and dependencies -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>