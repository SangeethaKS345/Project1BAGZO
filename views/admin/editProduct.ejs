

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    
    <!-- Cropper.js CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.12/cropper.min.css">
    
    <style>
        .image-actions {
            opacity: 0.8;
            transition: opacity 0.3s;
        }
        .image-preview-wrapper:hover .image-actions {
            opacity: 1;
        }
        .error-message {
            margin-top: 0.25rem;
        }
    </style>
    <%- include("../../views/partials/admin/header") %>
</head>
<body>
    <section class="content-main">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="content-header mb-4">
                    <h2 class="content-title">Edit Product</h2>
                </div>
                
                <!-- Remove debug card in production -->
                
                <div class="card mb-4 shadow-sm">
                    <div class="card-body p-4">
                        <form method="POST" action="/admin/editProduct/<%= product._id%>" enctype="multipart/form-data" id="productForm">
                            <div class="mb-4">
                                <label for="product_name" class="form-label fw-bold">Product Name</label>
                                <input type="text" name="productName" value="<%= product.productName %>" class="form-control border" id="product_name">
                                <!-- Store original image paths -->
                                <% for(let i=0; i<4; i++) { %>
                                    <input type="hidden" value="<%= product.productImage[i] || '' %>" name="image<%= i+1 %>" id="hiddenImage<%= i+1 %>">
                                <% } %>
                                <div id="productName-error" class="error-message text-danger small"></div>
                            </div>
                            
                            <div class="row mb-4">
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Brand</label>
                                    <select class="form-select border" name="brand" id="brand">
                                        <% for(let i=0; i<brand.length; i++) { %>
                                            <option value="<%= brand[i].brandName %>" <%= brand[i].brandName === product.brand ? 'selected' : '' %>>
                                                <%= brand[i].brandName %>
                                            </option>
                                        <% } %>
                                    </select>
                                    <div id="brand-error" class="error-message text-danger small"></div>
                                </div>
                                
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Category</label>
                                    <select class="form-select border" name="category" id="category">
                                        <% for(let i=0; i<cat.length; i++) { %>
                                            <option value="<%= cat[i].name %>" <%= cat[i].name === product.category ? 'selected' : '' %>>
                                                <%= cat[i].name %>
                                            </option>
                                        <% } %>
                                    </select>
                                    <div id="category-error" class="error-message text-danger small"></div>
                                </div>
                            </div>
                            
                            <div class="mb-4">
                                <label class="form-label fw-bold">Description</label>
                                <textarea name="descriptionData" class="form-control border" rows="4" id="description"><%= product.description %></textarea>
                                <div id="description-error" class="error-message text-danger small"></div>
                            </div>
                            
                            <div class="row mb-4">
                                <div class="col-md-4">
                                    <label class="form-label fw-bold">Regular Price (₹)</label>
                                    <input name="regularPrice" type="number" value="<%= product.regularPrice %>" class="form-control border" id="regularPrice">
                                    <div id="regularPrice-error" class="error-message text-danger small"></div>
                                </div>
                                
                                <div class="col-md-4">
                                    <label class="form-label fw-bold">Sale Price (₹)</label>
                                    <input name="salesPrice" type="number" value="<%= product.salesPrice %>" class="form-control border" id="salePrice">
                                    <div id="salesPrice-error" class="error-message text-danger small"></div>
                                </div>
                                
                                <div class="col-md-4">
                                    <label class="form-label fw-bold">Quantity</label>
                                    <input name="quantity" type="number" value="<%= product.quantity %>" class="form-control border" id="quantity">
                                    <div id="quantity-error" class="error-message text-danger small"></div>
                                </div>
                            </div>
                            
                            <div class="card mb-4 border">
                                <div class="card-header bg-light">
                                    <h4 class="mb-0 fw-bold">Product Images</h4>
                                    <small class="text-muted">Upload up to 4 images. Click on images to crop.</small>
                                </div>
                                
                                <div class="card-body">
                                    <div class="row">
                                        <% for(let i=0; i<4; i++) { %>
                                        <div class="col-md-6 mb-4">
                                            <div class="image-container bg-light p-2 rounded">
                                                <div class="image-preview-wrapper position-relative" 
                                                     style="height: 200px; display: flex; align-items: center; justify-content: center;">
                                                    <% 
                                                    // Fix the image path construction
                                                    const imageName = product.productImage[i] || '';
                                                    const imgSrc = imageName ? 
                                                       (imageName.startsWith('/') ? imageName : `/uploads/re-image/${imageName}`) : 
                                                       '/uploads/placeholder-image.jpg'; 
                                                    %>
                                                    <img src="<%= imgSrc %>" 
                                                         alt="Product image <%= i+1 %>" 
                                                         id="imgView<%= i+1 %>" 
                                                         class="img-fluid" 
                                                         style="max-height: 180px; max-width: 100%; object-fit: contain; cursor: pointer;"
                                                         onerror="this.onerror=null; this.src='/uploads/placeholder-image.jpg'">
                                                    
                                                    <div class="image-actions position-absolute bottom-0 end-0 p-2 bg-dark bg-opacity-75 rounded-start">
                                                        <button type="button" class="btn btn-sm btn-light me-1" onclick="openCropper('<%= i+1 %>')">
                                                            <i class="fas fa-crop-alt"></i> Crop
                                                        </button>
                                                        <button type="button" class="btn btn-sm btn-info me-1" onclick="openFilePicker('<%= i+1 %>')">
                                                            <i class="fas fa-upload"></i> Change
                                                        </button>
                                                        <button type="button" class="btn btn-sm btn-danger" onclick="confirmDeleteImage('<%= i+1 %>')">
                                                            <i class="fas fa-trash"></i>
                                                        </button>
                                                    </div>
                                                </div>
                                                
                                                <div class="mt-2">
                                                    <input class="form-control" 
                                                    type="file" 
                                                    name="images" 
                                                    id="input<%= i + 1 %>" 
                                                    accept="image/png, image/jpeg, image/jpg"
                                                    style="display: none;" 
                                                    onchange="handleImageSelect('<%= i + 1 %>')">
                                                    <div class="image-status mt-1 text-center small">
                                                        <% if (product.productImage[i]) { %>
                                                            <span class="badge bg-success" id="status<%= i+1 %>">Image <%= i+1 %> Loaded</span>
                                                        <% } else { %>
                                                            <span class="badge bg-secondary" id="status<%= i+1 %>">No Image</span>
                                                        <% } %>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="d-flex justify-content-between mb-3">
                                <a href="/admin/products" class="btn btn-outline-secondary">Cancel</a>
                                <button class="btn btn-primary" id="updateBtn" type="submit">
                                    <i class="fas fa-save me-1"></i> Update Product
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </section>
    
    <!-- Image Cropper Modal -->
    <div class="modal fade" id="imageCropperModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Crop Image</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="img-container" style="max-height: 500px;">
                        <img id="imageToCrop" src="" alt="Image to crop" style="display: block; max-width: 100%;">
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="button" class="btn btn-primary" id="cropImageBtn">Crop & Save</button>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Delete Confirmation Modal -->
    <div class="modal fade" id="deleteImageModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Confirm Delete</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p>Are you sure you want to delete this image?</p>
                    <p class="text-danger">This action cannot be undone.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="button" class="btn btn-danger" id="confirmDeleteBtn">Delete</button>
                </div>
            </div>
        </div>
    </div>

    <script>
    // Script to debug image loading issues
    document.addEventListener('DOMContentLoaded', function() {
        // Debug function to check image URLs
        function checkImageUrls() {
            for(let i = 1; i <= 4; i++) {
                const img = document.getElementById(`imgView${i}`);
                const status = document.getElementById(`status${i}`);
                const hiddenInput = document.getElementById(`hiddenImage${i}`);
                
                if(img) {
                    console.log(`Image ${i} src: ${img.src}`);
                    console.log(`Image ${i} hidden value: ${hiddenInput ? hiddenInput.value : 'not found'}`);
                    
                    // Try direct approach for image loading
                    img.onerror = function() {
                        console.error(`Failed to load image ${i}: ${this.src}`);
                        this.src = '/uploads/placeholder-image.jpg';
                        if(status) {
                            status.className = 'badge bg-danger';
                            status.textContent = 'Image Error';
                        }
                    };
                    
                    img.onload = function() {
                        console.log(`Successfully loaded image ${i}: ${this.src}`);
                        if(this.src.includes('placeholder-image.jpg')) {
                            if(status) {
                                status.className = 'badge bg-secondary';
                                status.textContent = 'No Image';
                            }
                        } else {
                            if(status) {
                                status.className = 'badge bg-success';
                                status.textContent = `Image ${i} Loaded`;
                            }
                        }
                    };
                    
                    // Try alternative methods to load images
                    if(hiddenInput && hiddenInput.value) {
                        const alternativePath = hiddenInput.value.startsWith('/') ? 
                            hiddenInput.value : 
                            `/uploads/re-image/${hiddenInput.value}`;
                        
                        // Create a test image to see if we can load it
                        const testImg = new Image();
                        testImg.onload = function() {
                            console.log(`Alternative path worked for image ${i}: ${alternativePath}`);
                            img.src = alternativePath;
                        };
                        testImg.onerror = function() {
                            console.error(`Alternative path failed for image ${i}: ${alternativePath}`);
                        };
                        testImg.src = alternativePath;
                    }
                }
            }
        }
        
        // Run the check
        setTimeout(checkImageUrls, 500);
    });
    </script>
</body>
</html>

    <%- include("../../views/partials/admin/footer") %>